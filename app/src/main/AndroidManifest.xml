<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grad.one4everything">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyTheme">
        <activity
            android:name=".activitypack.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".applock.AppLockChooserActivity"/>
        <activity android:name=".activitypack.MainActivity" />
        <activity android:name=".applock.AppLockActivity" />
        <activity android:name=".imagehider.HiderMainActivity" />
        <activity
            android:name=".activitypack.SecurityActivity"
            android:theme="@style/OneForEverything" />
    </application>

    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.REAL_GET_TASKS" />
</manifest>
